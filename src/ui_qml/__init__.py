"""QML desktop presentation layer.

This package is the final target for the QWidget-to-QML migration. Existing
`src.ui` Widget screens remain active until each screen is migrated, verified,
and deleted.
"""
