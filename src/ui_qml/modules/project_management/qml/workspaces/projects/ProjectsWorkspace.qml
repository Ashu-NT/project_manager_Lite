import QtQuick
import ProjectManagement.Widgets 1.0 as ProjectManagementWidgets

ProjectManagementWidgets.WorkspacePlaceholderPage {
    routeId: "project_management.projects"
    fallbackTitle: "Projects"
    fallbackSummary: "Project lifecycle, ownership, status, and project list workflows."
    fallbackLegacyRuntimeStatus: "Existing QWidget projects workspace remains active"
}
