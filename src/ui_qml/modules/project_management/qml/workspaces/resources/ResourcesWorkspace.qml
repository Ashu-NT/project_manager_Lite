import QtQuick
import ProjectManagement.Widgets 1.0 as ProjectManagementWidgets

ProjectManagementWidgets.WorkspacePlaceholderPage {
    routeId: "project_management.resources"
    fallbackTitle: "Resources"
    fallbackSummary: "Resource capacity, allocation, project assignments, and utilization views."
    fallbackLegacyRuntimeStatus: "Existing QWidget resources workspace remains active"
}
