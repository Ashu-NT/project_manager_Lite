import QtQuick
import InventoryProcurement.Widgets 1.0 as InventoryWidgets

InventoryWidgets.WorkspacePlaceholderPage {
    routeId: "inventory_procurement.inventory"
    fallbackTitle: "Inventory"
    fallbackSummary: "Storerooms, stock balances, adjustments, issues, returns, and transfer movements."
    architectureSummary: "The split stock, movement, and storeroom desktop APIs are ready for the next inventory workflow migration slice."
}
