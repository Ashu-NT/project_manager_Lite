import QtQuick
import InventoryProcurement.Widgets 1.0 as InventoryWidgets

InventoryWidgets.WorkspacePlaceholderPage {
    routeId: "inventory_procurement.reservations"
    fallbackTitle: "Reservations"
    fallbackSummary: "Reservation holds, issuing, release flows, and source-reference stock demand."
    architectureSummary: "Reservation desktop APIs are already isolated and will migrate onto this page in the next reservation slice."
}
