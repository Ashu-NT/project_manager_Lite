import QtQuick
import InventoryProcurement.Widgets 1.0 as InventoryWidgets

InventoryWidgets.WorkspacePlaceholderPage {
    routeId: "inventory_procurement.pricing"
    fallbackTitle: "Pricing"
    fallbackSummary: "Pricing and supplier analysis will land here once the later pricing slice is transferred."
    architectureSummary: "Pricing remains intentionally deferred until its backend slice is transferred."
}
