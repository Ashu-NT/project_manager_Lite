"""PM-owned gateway ports for capabilities other bounded-context modules
provide (e.g. Inventory/Procurement). See `gateway.task.reservation` for
the first example -- structural `Protocol` types PM defines and depends
on, satisfied by another module's existing class with no import in either
direction (see TODO §5A)."""
